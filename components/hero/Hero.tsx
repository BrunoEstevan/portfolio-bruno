"use client";

import React from "react";
import HeroContent from "./HeroContent";
import HeroContainer from "./HeroContainer";

const Hero = () => {
  return (
    <HeroContainer>
      <HeroContent />
    </HeroContainer>
  );
};

export default Hero;
